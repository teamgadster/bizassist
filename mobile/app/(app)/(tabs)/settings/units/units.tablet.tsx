// BizAssist_mobile
// path: app/(app)/(tabs)/settings/units/units.tablet.tsx

import { UnitsLedgerScreen } from "./units.ledger";

export default function UnitsTabletScreen() {
	return <UnitsLedgerScreen layout='tablet' />;
}
